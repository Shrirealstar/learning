//indexc.js
import React from 'react';
import ReactDOM from 'react-dom';
import App from './front.jsx'; // Assuming that your front.jsx file is in the same directory

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
